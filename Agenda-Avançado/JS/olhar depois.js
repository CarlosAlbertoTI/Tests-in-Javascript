var notas = {
				anotacao : null,
				hora :  null,
				data :  null,
				//caso a data da nota ainda esta por vir true, caso não false
				status : true,
				handleEvent : function(e){ if (e.type == 'segundo') {
if e.tempo == this.data 

				}}
			}
				//depois esse objeto deve ser retornado para a função validacaoDatas atraves de um array
			
			
// criando um array onde vai ficar todos os obejtos para depois acessa-los para verificação das datas e horas armazenadas
var lista_objetos = [];